package config

import "fmt"

// This file will config (redis, rabbitMQ, and mongoDB) also will config with
// gmail provider for now and will provide a lossley coupled interface to the other service and logic

func Test() {
	fmt.Printf("test\n")
}
